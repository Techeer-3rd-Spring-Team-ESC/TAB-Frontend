import {StyleSheet, Platform} from 'react-native';

const styles = StyleSheet.create ({
    buttonStyle: {
        marginLeft: 320,
        marginRight: 10,
        padding: 30,
        position: 'absolute',
        bottom: -10
    }
});

export default styles;