import {StyleSheet, Platform} from 'react-native';

const styles = StyleSheet.create ({
    buttonStyle: {
        marginLeft: 340,
        marginRight: 10,
        padding: 10,
        position: 'absolute',
        bottom: -10
    }
});

export default styles;